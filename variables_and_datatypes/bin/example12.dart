void main(List<String> args) {
  const String yourName = 'Foo Bar';
  final hisName = yourName; //Optional to sepcify type, But teams might agree to always specify. 
  print(yourName);
  print(hisName);
}